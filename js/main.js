window.onload = function() {
    'use strict';

    /*
     * main.js - entry point
     *
     * This is the final module, loaded after everything else
     * It creates a new game instance when the DOM is loaded and ready to go
     * Then the game.js module takes it from there
     *
     */
    
    new GAME();
    
};